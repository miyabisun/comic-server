services:
  comic-server:
    image: ghcr.io/miyabisun/comic-server:latest
    ports:
      - "3000:3000"
    volumes:
      - /path/to/comics:/comics
    environment:
      - COMIC_PATH=/comics
      - PORT=3000
